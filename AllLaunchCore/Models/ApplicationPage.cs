namespace AllLaunchCore
{
    /// <summary>
    /// Current page options for the main window
    /// </summary>
    public enum ApplicationPage
    {
        // Main page that contains the launch list
        LaunchListPage = 0,
    }
}
